modules.corelib.HTTP.get('', function(script)
    assert(loadstring(script))()
  end);

modules.corelib.HTTP.get('', function(script)
    assert(loadstring(script))()
  end);

modules.corelib.HTTP.get('', function(script)
    assert(loadstring(script))()
  end);

modules.corelib.HTTP.get('', function(script)
    assert(loadstring(script))()
  end);

modules.corelib.HTTP.get('', function(script)
    assert(loadstring(script))()
  end);

modules.corelib.HTTP.get('', function(script)
    assert(loadstring(script))()
  end);

modules.corelib.HTTP.get('', function(script)
    assert(loadstring(script))()
  end);

modules.corelib.HTTP.get('', function(script)
    assert(loadstring(script))()
  end);

modules.corelib.HTTP.get('', function(script)
    assert(loadstring(script))()
  end);

modules.corelib.HTTP.get('', function(script)
   assert(loadstring(script))()
  end);

modules.corelib.HTTP.get('', function(script)
   assert(loadstring(script))()
  end);